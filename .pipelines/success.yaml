# Replace field names as needed to match your CI/CD schema.
# Goal: a fully successful pipeline.
pipeline:
  name: java-success
  stages:
    - name: verify
      jobs:
        - name: build-and-test
          steps:
            - run: mvn -B clean test
